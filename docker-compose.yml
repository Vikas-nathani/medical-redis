version: "3.9"

services:
  redis:
    image: redis:latest
    container_name: redis-medical
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes", "--appendfsync", "everysec"]
    volumes:
      - redis_data:/data

volumes:
  redis_data: 